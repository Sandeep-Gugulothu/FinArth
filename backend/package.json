{
  "name": "finarth-backend",
  "version": "1.0.0",
  "description": "FinArth Backend API (Python)",
  "main": "src/server.py",
  "scripts": {
    "dev": "cd src && python server.py",
    "start": "cd src && python server.py",
    "install": "pip install -r requirements.txt",
    "test": "python -m pytest",
    "db:view": "node scripts/db-viewer.js"
  },
  "dependencies": {
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "@types/node": "^20.4.0",
    "@types/sqlite3": "^3.1.8"
  }
}